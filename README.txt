Requirements:

python3-dev

Building:

./setup.sh <root of brewpi-avr directory, see https://github.com/BrewPi/brewpi-avr>
make

Running make will produce a TempControl.so that can be imperted by python3, see example for usage
